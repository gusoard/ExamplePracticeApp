<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="personal.gusorivera.examplepracticeapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ExampleSelection">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RecycleViewExample" />
        <activity android:name=".SimpleListViewExample" />
        <activity android:name=".SharedPrefsExample" />
        <activity android:name=".ChoresExample" />
        <activity android:name=".ui.ChoresList" />
        <activity android:name=".ui.ChoresListExample" />
        <activity android:name=".ChoresList"></activity>
    </application>

</manifest>